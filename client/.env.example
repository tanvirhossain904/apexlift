# Copy to .env.local for local dev (optional — defaults to '/api' which uses Vite's dev proxy)
# In production, set this on your hosting platform (e.g. Vercel project env vars)
# Example: https://apexlift.onrender.com/api
VITE_API_URL=
